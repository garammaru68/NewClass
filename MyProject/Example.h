#pragma once
#include <iostream>
#include <cmath>
#include <algorithm>
#include <string>
#include <vector>

bool checkPalindrome(std::string inputString);
//bool almostIncreasingSequence(std::vector<int> sequence);
bool almostIncreasingSequence(std::vector<int> sequence);